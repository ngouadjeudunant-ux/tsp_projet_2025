/* dist_matrix.c
 * Construit et gère la demi-matrice (lower-triangular) des distances.
 * Fonction principale : dm_build(Instance*, dist_fct) et accès dm_get(dm,i,j).
 * But : calculer chaque paire une fois pour accélérer les algorithmes.
 */

